import 'package:better_munchkin/utils/custom_button.dart';
import 'package:flutter/material.dart';

class Strategic extends StatelessWidget {
  const Strategic({super.key});

// TODO: try https://pub.dev/packages/flutter_staggered_grid_view
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        // child: Center(
        //   child: CustomButton(text: 'Add Player'),
        // ),
      ),
    );
  }
}
